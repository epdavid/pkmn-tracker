import { FunctionComponent } from 'react'
import Typography from '@material-ui/core/Typography'


const Header: FunctionComponent<{}> = ({}) => {
    return (
        <div className="Header">
            <Typography variant="h2">wartortle</Typography>
        </div>
    )
}

export default Header